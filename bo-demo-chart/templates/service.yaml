apiVersion: v1
kind: Service
metadata:
  name: {{ include "bo-demo-chart.fullname" . }}
  labels:
    {{- include "bo-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bo-demo-chart.selectorLabels" . | nindent 4 }}
